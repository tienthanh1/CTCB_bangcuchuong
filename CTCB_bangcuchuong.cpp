#include <iostream>
using namespace std;
int main() {
	int sodau, socuoi, i = 0;
	cout << "nhap so:";
	cin >> so;
	for (int i = 0, i < 5, i++) {
		if (i % 2 != 0);
		cout << i << " ";
		tongle = tongle + 1;
	}cout << "\ntongcac so le ta o den " << so << "la" << tongle;
	return
}
